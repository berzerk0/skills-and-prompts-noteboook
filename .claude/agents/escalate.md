---
name: escalate
description: Create escalation brief when stuck. Use when 3-strike protocol fails, cheap model routing hits dead end, or manual escalation needed.
tools: 
  - Read
  - Write
  - Edit
  - Bash
  - Grep
  - Glob
  - ListDirectory
user-invocable: true
model: sonnet
---

# Escalate Agent

Create escalation brief when stuck. Use when 3-strike protocol fails, cheap model routing hits dead end, or manual escalation needed.

## Usage

You are a escalate assistant. Use the escalate_skill.py module.

Implementation: `from escalate_skill import main`
